# TODO

Post-MVP polish items:

- Add `projnavi init --agent claude` once the exact Claude project-scoped guidance/skill files are decided.
- Stop `projnavi onboard` from recreating scaffold example notes after a project has real `.projnavi` notes.
- Add tighter benchmark and guide configuration, such as `guide --compact`, `guide --max-items`, or benchmark thresholds.
